import { HidenavContentDirective } from './hidenav-content.directive';

describe('HidenavContentDirective', () => {
  it('should create an instance', () => {
    const directive = new HidenavContentDirective();
    expect(directive).toBeTruthy();
  });
});
