import { HidenavHeaderDirective } from './hidenav-header.directive';

describe('HidenavHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new HidenavHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
